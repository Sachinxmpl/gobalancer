# example configuration file 

mode: l7
listen: "127.0.0.1:8443"
tls:
  cert: "deploy/dev.crt"
  key: "deploy/dev.key"
routes:
  - match: { path_prefix: "/" }
    pool: default
pools:
  default:
    - addr: "127.0.0.1:9001"
